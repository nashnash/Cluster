{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
    <h1>Edit Event</h1>

    {% if event.user == app.user %}

        {% if event.user.roles != 'ROLE_USER' %}

            {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('event_index') }}" class="btn btn-outline-primary">back to list</a>

            {{ include('event/_delete_form.html.twig') }}

        {% endif %}

    {% endif %}
{% endblock %}
